# Claude CLI auth (from `claude setup-token`)
CLAUDE_CODE_OAUTH_TOKEN="sk-ant-oat01-..."

# GitHub auth token (from `gh auth token`)
GH_TOKEN=

# Optional: override claude binary path
# CLAUDE_BIN="/path/to/claude"
