# Builder Agent Memory

Persistent lessons for implementation flow, issue triage, and PR execution quality.

- (init) Prioritize one PR per issue, avoid duplicate branches/PRs, and integrate reviewer feedback into follow-up commits.
- 2026-03-04: Repo alexjaniak/DACL had one open issue (#1), but an open PR already references it (PR #3). Correct behavior is silent skip with no new PR/comment; continue scanning other candidates/repos.

- 2026-03-04: Builder loop run at 19:16 UTC scanned alexjaniak/DACL issue #1; open PR #3 already linked via body search (#1), so skipped silently per policy. No additional actionable unassigned issues found.
- 2026-03-04: Builder loop run at 19:21 UTC re-validated alexjaniak/DACL issue #1 is already covered by open PR #3 (`agent/dacl-builder-1-subagent-wallet-flow`); skipped with no duplicate PR/comment. Branch hygiene note: builder worktree currently has uncommitted changes on the linked branch (`scripts/setup-subagent.sh`, `agents/metadata/`), so future loops should avoid cross-issue work until that branch is cleaned/merged.

- 2026-03-04 19:26 UTC: Builder loop found no open issues in alexjaniak/DACL (`gh issue list` returned empty). Blocker: none. Branch hygiene: builder worktree remains dirty on `agent/dacl-builder-1-subagent-wallet-flow` (modified `scripts/setup-subagent.sh`, untracked `agents/metadata/`), so avoid starting new issue branches from that worktree until cleaned.
- 2026-03-04 19:32 UTC: Builder loop re-ran target scan; `alexjaniak/DACL` still has no open issues (empty `gh issue list`). No claim comments, branches, PRs, or issue updates created. Branch hygiene unchanged: builder worktree is still dirty on `agent/dacl-builder-1-subagent-wallet-flow`; keep future runs read-only until this branch is cleaned or merged.
- 2026-03-04 19:37 UTC: Builder loop scanned targets from `agents/config/review-targets.txt`; `alexjaniak/DACL` returned no open issues via `gh issue list --state open`. Result: no PR creation/follow-up work, no comments posted, duplicate-skip logic not triggered.
- 2026-03-04 19:41 UTC: Cron builder loop run (`fb6129e3-7b17-4d6d-9e62-b748d7acc262`) checked configured targets; `alexjaniak/DACL` has zero open issues and zero open PRs. No candidate issue to claim, no duplicate PR condition encountered, and no issue/PR comments were posted.
- 2026-03-04 19:47 UTC: Cron builder loop run (`fb6129e3-7b17-4d6d-9e62-b748d7acc262`) scanned `agents/config/review-targets.txt` (alexjaniak/DACL). `gh issue list --state open` and `gh pr list --state open` both returned empty arrays, so no issue claim, no PR creation, no follow-up commits, and no comments with prefix `[agent:dacl-builder-agent]` were needed.
- 2026-03-04 19:52 UTC: Cron builder loop run (`fb6129e3-7b17-4d6d-9e62-b748d7acc262`) re-scanned configured targets; `alexjaniak/DACL` still reports no open issues and no open PRs. No duplicate checks triggered, no PR/branch work performed, and no `[agent:dacl-builder-agent]` comments posted.
- 2026-03-04 19:56 UTC: Builder loop found new open issue `alexjaniak/DACL#4` with no linked open PR, created branch `agent/dacl-builder-4-devnet-bootstrap`, implemented Solana devnet bootstrap flow (config template, Rust SDK crate + CLI, setup wrapper, optional setup-subagent integration, README docs), pushed and opened PR #5, then posted prefixed status comments on issue/PR (`[agent:dacl-builder-agent]`).
- 2026-03-04 20:05 UTC: Cron builder loop checked active PR #5 for issue/PR drift via `./scripts/check-issue-pr-sync.sh alexjaniak/DACL 5`; result `IN_SYNC` with issue #4, so no follow-up commit/comment needed and duplicate PR creation remained skipped.
- 2026-03-04 20:40 UTC: Builder loop scanned `alexjaniak/DACL` and found open PR #5 plus open issues #4/#6. PR #5 had no new unresolved actionable items beyond already-addressed thread items, so no additional commit/comment posted. Issue #6 had no active implementing PR, so I claimed it, created branch `agent/dacl-builder-6-nextjs-dashboard`, implemented a minimal Next.js dashboard (`dashboard/`) with agents/cron visibility, added `agents/config/cron-jobs.json`, pushed commit `a31573c`, and opened PR #7 with `Closes #6` + checklist.
- 2026-03-04 20:45 UTC: Builder loop scanned alexjaniak/DACL open PRs #5/#7 and open issues #4/#6. Found actionable unresolved item on PR #5: failing verify check due pinned Rust 1.85.0 while transitive `time` deps require rustc 1.88.0. Checked out `agent/dacl-builder-4-devnet-bootstrap`, pushed commit `346696b` raising toolchain/preflight/docs minimum to 1.88.0, then posted PR update comment with evidence + blocker-state change. No new work claimed because both open issues already have active implementing PRs (#5 for #4, #7 for #6).
- 2026-03-04 20:58 UTC: Builder loop re-checked alexjaniak/DACL open PRs #5/#7 and open issues #4/#6. Ran `./scripts/check-issue-pr-sync.sh` for both PRs (both `IN_SYNC`), reviewed PR + linked issue threads, and found no unresolved fixable action items left: PR #5 latest CI/check blockers are resolved at head (`verify` success) and PR #7 has no outstanding change requests. Since both open issues already have active implementing PRs, no new issue was claimed, no code commit was needed, and no no-op comment was posted.
- 2026-03-04 21:04 UTC: Cron builder loop scanned `alexjaniak/DACL` with open PRs #5/#7 and open issues #4/#6. Re-ran `./scripts/check-issue-pr-sync.sh` for both PRs (`IN_SYNC`), reviewed PR+linked issue comments, and confirmed no new unresolved actionable/fixable items: both PR check suites are green (`solana-bootstrap-sdk/verify`, `dashboard-verify`) and no new blocker/change-request comments were added after latest approvals. Because each open issue already has an active implementing PR, no new issue was claimed, no commits were pushed, and no comments were posted.
- 2026-03-04 21:09 UTC: Cron builder loop (`fb6129e3-7b17-4d6d-9e62-b748d7acc262`) refreshed `main`, read builder instructions/memory/targets, and scanned `alexjaniak/DACL` open PRs (#5, #7) + issues (#4, #6). Ran issue/PR sync checks for both PRs (`IN_SYNC`), reviewed PR and linked issue threads, and found no new unresolved actionable items requiring code changes. Both PR verify checks are green, and every open issue already has an active implementing PR; therefore no claim/branch/commit/comment actions were taken.
- 2026-03-04 21:18 UTC: Cron builder loop refreshed main, read builder directives, scanned target `alexjaniak/DACL`, listed open PRs (#5, #7) and open issues (#4, #6), ran `./scripts/check-issue-pr-sync.sh` for both PRs (`IN_SYNC`), and reviewed PR + linked issue comment threads. No new unresolved actionable/fixable items were found (PR #5 verify check is green; PR #7 has no outstanding change requests), and each open issue already has an active implementing PR, so no commits/comments/claims were made.
- 2026-03-04 21:23 UTC: Cron builder loop re-synced `main`, read builder instructions/memory/targets, and scanned `alexjaniak/DACL` open PRs (#5, #7) plus open issues (#4, #6). For each PR, reviewed thread + linked issue comments and ran `./scripts/check-issue-pr-sync.sh` (both `IN_SYNC`). No unresolved actionable/fixable items remained (PR #5 has passing `solana-bootstrap-sdk / verify`; PR #7 has no outstanding review requests/comments), and all open issues already have active implementing PRs, so no branch work, commits, pushes, or comments were made.
